# Auto detect text files and perform LF normalization
* text=auto eol=lf
.gitignore export-ignore
.gitattributes export-ignore

# Explicitly declare binary files so they aren't corrupted by line-ending normalization
*.png binary
*.ico binary
*.icns binary
*.pdf binary